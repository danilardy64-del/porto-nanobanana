import { PortfolioItem } from "../types";

// File ini awalnya kosong.
// Setelah Anda upload gambar di Localhost, klik tombol "DOWNLOAD DATA DEPLOY" (Admin Only).
// Copy isi file yang didownload, lalu paste (overwrite) ke file ini.
// Saat di-deploy ke Netlify, gambar akan muncul otomatis.

export const INITIAL_DATA: PortfolioItem[] = [];